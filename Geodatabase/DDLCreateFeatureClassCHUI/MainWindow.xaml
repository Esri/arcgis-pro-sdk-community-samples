<!--

   Copyright 2026 Esri

   Licensed under the Apache License, Version 2.0 (the "License");
   you may not use this file except in compliance with the License.
   You may obtain a copy of the License at

       https://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.

   See the License for the specific language governing permissions and
   limitations under the License.

-->
<Window x:Class="DDLCreateFeatureClassCHUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DDLCreateFeatureClassCHUI"
        xmlns:VM="clr-namespace:DDLCreateFeatureClassCHUI.ViewModel"
        xmlns:converters="clr-namespace:DDLCreateFeatureClassCHUI.Converters"
        mc:Ignorable="d"
        Title="Create Feature Class" Height="600" Width="800">
  <Window.Resources>
    <converters:StepIndicatorConverter x:Key="StepIndicatorConverter"/>
    <converters:StepTitleConverter x:Key="StepTitleConverter"/>
    <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
  </Window.Resources>
  <Window.DataContext>
    <VM:MainWindowVM/>
  </Window.DataContext>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Background="#2D5B96" Padding="16" Grid.Row="1">
      <TextBlock Text="{Binding CurrentStepIndex, Converter={StaticResource StepTitleConverter}}" 
                       Foreground="White" FontSize="20" FontWeight="Bold"/>
    </Border>

    <!-- Content Area -->
    <ContentControl Grid.Row="2" Margin="16"
                        Content="{Binding CurrentStepView}"/>

    <!-- Navigation Buttons -->
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="3" Margin="16">
      <Button Content="Back" Width="80" Margin="4" Command="{Binding BackCommand}" IsEnabled="{Binding CanGoBack}"/>
      <Button Content="Next" Width="80" Margin="4" Command="{Binding NextCommand}" Visibility="{Binding ShowFinish, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter='Invert'}" IsEnabled="{Binding CanGoNext}"/>
      <Button Content="Finish" Width="80" Margin="4" Command="{Binding FinishCommand}" Visibility="{Binding ShowFinish, Converter={StaticResource BooleanToVisibilityConverter}}" IsEnabled="{Binding CanFinish}"/>
      <Button Content="Cancel" Width="80" Margin="4" Command="{Binding CancelCommand}"/>
    </StackPanel>
  </Grid>
</Window>
